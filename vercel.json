{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "flutter build web --release --web-renderer html --base-href /",
  "outputDirectory": "build/web",
  "installCommand": "flutter config --enable-web && flutter pub get"
}